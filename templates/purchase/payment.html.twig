{% extends "base.html.twig" %}

{% block title %}
    Payez votre commande avec Stripe
{% endblock %}

{% block body %}

    <main class="container py-5">
    
        <h1>Payez votre commande avec Stripe</h1>

        <form id="payment-form">
            <div id="payment-element">
                <!-- Elements will create form elements here -->
            </div>
            <button id="submit" class="btn btn-success mt-3">Payer avec Stripe</button>
            <div id="payment-message" class="hidden"></div>
        </form>

    </main>

{% endblock %}


{% block javascripts %}
    {# Balise Parent : pour conserver ce qu'il pourrait déjà y avoir dans le block JS  sans l'écraser #}
    {{ parent() }}

    {# Import JS Stripe #} 
    <script src="https://js.stripe.com/v3/"></script>
    <script>
        const clientSecret = '{{ clientSecret }}';
        const stripePublicKey = '{{ stripePublicKey }}';
        const redirectAfterSuccessUrl = "{{ url('purchase_payment_success', {id:  purchase.id }) }}"
    </script>
    <script src="/js/payment.js"></script>

{% endblock %}
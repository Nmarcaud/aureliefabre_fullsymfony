<!DOCTYPE html>
<html lang="fr">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <title>Chèque Cadeau</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/eb0fe598e4.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="{{ absolute_url('/css/style.css') }}">

</head>
<body>

    {% for item in purchase.purchaseItems %}
        <div class="card">
            <div class="card-body">
                <picture>
                    <img src="{{ absolute_url(item.product.mainPicturePath)  }}" alt="">
                </picture>
                <div class="content">
                    <p class="mb-0 green-2">Prestation offerte</p>
                    <h2>{{ item.productName }}</h2>

                    <p class="mb-0 dg-2">Durée</p>
                    <h2>{{ item.product.duration | duration }}</h2>
                </div>
            </div>
        </div>
    {% endfor %}

    <div class="card">
        <div class="card-body bg-secondary">

            <img src="https://picsum.photos/id/1/200/300" alt="">

            <div class="content">
                <p class="mb-0 dg-2">Prestation offerte</p>
                <h2>Nom du produit</h2>

                <p class="mb-0 dg-2">Durée</p>
                <h2>30min</h2>
            </div>

        </div>
    </div>

    <table>
        <thead>
            <tr>
                <th colspan="2">The table header</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>The table body</td>
                <td>with two columns</td>
            </tr>
        </tbody>
    </table>

    {# <pre>
        {{ dump(purchase) }}
    </pre> #}
</body>
</html>  

<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}

            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

            <script src="https://kit.fontawesome.com/eb0fe598e4.js" crossorigin="anonymous"></script>
            {# {{ encore_entry_link_tags('app') }} #}
        {% endblock %}

        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
            {# {{ encore_entry_script_tags('app') }} #}

        {% endblock %}
    </head>
    
    <body>
        {# Primary Nav #}
        {% include "partials/nav/_primary_navbar.html.twig" %}

        {# Secondary Nav #}
        {% include "partials/nav/_secondary_navbar.html.twig" %}


        <div class="container pt-3">

            {# Notifications #}
            {% for rubrique, messages in app.flashes %}
                <div class="alert alert-{{ rubrique }}" role="alert">
                    {% for m in messages %}
                        {{ m }}
                    {% endfor %}
                </div>
            {% endfor %}

            {# Toast avec Symfony Turbo/encore... #}
            {# https://symfonycasts.com/screencast/turbo/toast #}
            {# 
                Remettre en place les balises nécessaires à Encore dans le fichier base.html.twig 
                Faire tout ce qu'il faut pour que Encore ou Turbo fonctionne
                Puis re-suivre la procédure du lien ci-dessus
            #}
            {# {% for flash in app.flashes %}
                {{ include('partials/_toast.html.twig', {
                    title: 'It Works !',
                    body: flash
                }) }}
            {% endfor %} #}

            {% block body %}{% endblock %}
        </div>
        
    </body>
</html>
